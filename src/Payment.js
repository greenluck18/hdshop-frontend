import './App.css';
import { useEffect, useState } from 'react';

function Payment() {

    

  const onButtonClick =  async () => {
    
  };

  return (
    <button id={"payButton"} type="button" onClick={onButtonClick} value="My Items">Pay</button>
  );
}

export default Payment;